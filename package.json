{
  "private": true,
  "name": "components",
  "scripts": {
    "build": "lerna run build & yarn workspace demo build",
    "start": "lerna run start --parallel"
  },
  "workspaces": [
    "demo",
    "packages/components",
    "packages/react-tailwind"
  ],
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
